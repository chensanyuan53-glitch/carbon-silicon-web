# 任务完成确认功能使用说明

## 功能概述

实现了任务完成确认功能，要求接单者和发布者都确认完成，任务才算真正完成。

## 功能流程

### 1. 接单者确认完成
- **位置**：个人中心 → 我的接单
- **触发条件**：接单者完成任务后，点击任务卡片右下角的"确认完成"按钮
- **流程**：
  1. 接单者点击"确认完成"按钮
  2. 弹出确认对话框："确认完成后，将通知发布者进行确认。请确保任务确实已完成。"
  3. 接单者确认后，系统自动向发布者发送通知

### 2. 发布者收到通知
- **位置**：右上角通知图标
- **通知内容**：接单者已确认完成任务「任务标题」，请查看并确认。
- **触发方式**：点击通知

### 3. 发布者确认完成
- **位置**：个人中心 → 我的发布
- **触发条件**：发布者点击通知后，会跳转到任务页面
- **流程**：
  1. 系统自动切换到"我的发布"标签
  2. 显示任务卡片的"确认完成"按钮（蓝色）
  3. 点击后弹出确认对话框："确认完成后，任务将被标记为已完成。请确保检查任务完成情况。"
  4. 发布者确认后，任务状态更新为"已完成"

### 4. 任务状态显示
- **已完成**：绿色标签 "✓ 已完成"
- **等待发布者确认**：蓝色标签 "● 等待发布者确认"
- **正常进行中**：显示"立即接单"按钮

## 数据库变更

### 新增表字段

#### tasks_reward 表
```sql
confirmed_by_claimant BOOLEAN DEFAULT FALSE  -- 接单者是否确认完成
confirmed_by_publisher BOOLEAN DEFAULT FALSE  -- 发布者是否确认完成
completed_at TIMESTAMP WITH TIME ZONE           -- 任务完成时间
completed_by_user_id UUID                   -- 最后确认完成的用户ID
```

#### task_claims 表
```sql
claimant_confirmed BOOLEAN DEFAULT FALSE       -- 接单者是否确认完成
publisher_confirmed BOOLEAN DEFAULT FALSE       -- 发布者是否确认完成
claimant_confirmed_at TIMESTAMP WITH TIME ZONE  -- 接单者确认时间
publisher_confirmed_at TIMESTAMP WITH TIME ZONE  -- 发布者确认时间
```

### 自动触发器

1. **handle_claimant_completion()**：
   - 当接单者确认完成时，自动更新 tasks_reward 表
   - 当发布者确认完成时，更新任务完成状态
   - 当双方都确认时，标记任务为已完成

2. **notify_task_completion()**：
   - 接单者确认完成时，向发布者发送通知
   - 发布者确认完成时，向接单者发送通知

## 使用步骤

### 第一步：应用数据库迁移
```bash
# 运行数据库迁移
cd supabase/migrations
# 在 Supabase SQL Editor 中执行 20250126000004_add_task_completion_fields.sql
```

### 第二步：测试接单者确认完成
1. 使用接单者账号登录
2. 进入"任务大厅" → "个人中心" → "我的接单"
3. 找到已接单的任务
4. 点击任务右下角的"确认完成"按钮
5. 确认弹窗中的内容

### 第三步：测试发布者确认完成
1. 使用发布者账号登录
2. 等待接单者确认完成的通知（或手动触发）
3. 点击右上角通知图标
4. 点击任务完成确认通知
5. 系统会自动跳转到任务页面并显示确认按钮
6. 点击"确认完成"按钮确认

## 界面显示

### 接单者视角
- **我的接单页面**：
  - 未确认：显示绿色"确认完成"按钮
  - 已确认：显示蓝色"等待发布者确认"标签

### 发布者视角
- **我的发布页面**：
  - 等待确认：显示蓝色"确认完成"按钮
  - 已完成：显示绿色"✓ 已完成"标签

## 注意事项

1. **双方确认机制**：只有接单者和发布者都确认，任务才算完成
2. **通知系统**：每次确认都会通过通知系统通知对方
3. **状态同步**：任务状态会实时更新到数据库
4. **权限控制**：
   - 接单者只能确认自己接单的任务
   - 发布者只能确认自己发布的任务
5. **完成时间**：记录双方确认的时间，方便追溯

## 技术实现

### 前端组件
- **App.tsx**：处理通知跳转逻辑
- **Tasks.tsx**：任务列表和完成确认逻辑
- **Navbar.tsx**：传递通知回调
- **MessageDropdown.tsx**：通知显示和处理

### 后端逻辑
- **数据库触发器**：自动处理状态更新和通知发送
- **RLS 策略**：确保用户只能操作自己的任务和接单记录

## 故障排查

### 问题：看不到"确认完成"按钮
**解决方法**：
1. 确认是否已应用数据库迁移
2. 检查用户身份是否正确
3. 刷新页面重试

### 问题：点击通知没有跳转
**解决方法**：
1. 检查浏览器控制台是否有错误
2. 确认 localStorage 是否正常工作
3. 手动切换到"个人中心 → 我的发布"

### 问题：确认后状态没有更新
**解决方法**：
1. 检查数据库触发器是否正常工作
2. 查看浏览器网络请求是否成功
3. 刷新页面重试

## 后续优化建议

1. **添加完成证明**：可以添加上传完成截图或文件的步骤
2. **评分系统**：完成后可以让双方互相评分
3. **争议处理**：添加争议解决机制
4. **历史记录**：记录所有操作日志
